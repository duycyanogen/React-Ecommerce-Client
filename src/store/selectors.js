export const activePageSelector = (state) => state.activePage.activePage;
export const statisticsSelector = (state) => state.statistics.statistics;
export const searchSelector = (state) => state.search.search;
